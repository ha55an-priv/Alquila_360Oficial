export interface OwnerProfile {
  ci: string;
  fullName: string;
  birthDate: string;
  phone: string;
  email: string;
  totalProperties: number;
  activeContracts: number;
  rating: number; // 0 a 5
  totalIncome: number;
  rentedProperties: number;
}